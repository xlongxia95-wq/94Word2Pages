# 📚 講義轉換工具 v2.0

陳建豪物理 - 小豪編輯部

## 功能

將 Word 講義（.docx）自動轉換為：
- 📄 Markdown（可編輯純文字）
- 📄 RTF（Pages 可開）
- 📄 Pages（macOS 原生格式）

## 安裝

工具已安裝在 `~/.openclaw/workspaces/xiaohao/`

### 必要條件
- ✅ Python 3.11+
- ✅ PaddleOCR（已安裝在 ocr-env）
- ✅ Playwright（已安裝 Chromium）
- ✅ pandoc（Homebrew）
- ✅ Pages.app（自動轉換需要）

## 使用方式

### 方式 1: 快捷指令

```bash
cd ~/.openclaw/workspaces/xiaohao
./scripts/convert.sh 講義.docx
./scripts/convert.sh 講義.docx --pages  # 自動轉 Pages
```

### 方式 2: Python 直接執行

```bash
cd ~/.openclaw/workspaces/xiaohao
source ocr-env/bin/activate
python3 scripts/convert_lecture.py 講義.docx -o 輸出目錄
```

### 方式 3: 批次處理

```bash
python3 scripts/convert_lecture.py --batch ./講義資料夾/ -o ./輸出目錄/
```

## 參數說明

| 參數 | 說明 |
|------|------|
| `input` | Word 檔案路徑或目錄（配合 --batch） |
| `-o, --output` | 輸出目錄（預設：原檔名_output） |
| `--batch` | 批次處理目錄內所有 .docx |
| `--pages` | 自動轉換為 Pages 格式 |

## 輸出檔案

```
輸出目錄/
├── page_001.png        # 截圖（分頁）
├── page_002.png
├── 講義.html           # HTML 中間檔
├── 講義.md             # Markdown（主要輸出）
├── 講義.rtf            # RTF 格式
├── 講義.pages          # Pages 格式（需 --pages）
└── conversion_result.json
```

## 轉換流程

```
Word (.docx)
    ↓ textutil
HTML
    ↓ Playwright (headless Chromium)
PNG 截圖（分頁）
    ↓ PaddleOCR
純文字 + 座標
    ↓ 智慧格式化
Markdown
    ↓ pandoc
RTF
    ↓ AppleScript (可選)
Pages
```

## 智慧格式化

- ✅ 自動偵測標題（一、二、三...）
- ✅ 自動偵測 Example 區塊
- ✅ 自動偵測選項（A)(B)(C)...）
- ✅ 自動偵測解說區塊
- ✅ 物理公式轉 LaTeX（基礎支援）

## 範例

```bash
# 單檔轉換
./scripts/convert.sh samples/力學講義.docx

# 自動轉 Pages
./scripts/convert.sh samples/電磁學.docx --pages

# 批次處理整個目錄
./scripts/convert.sh --batch ./本學期講義/ -o ./轉換結果/
```

## 注意事項

1. **首次執行較慢**：OCR 模型需要載入（約 10 秒）
2. **圖片品質**：原始 Word 圖片會遺失，OCR 只能辨識文字
3. **公式限制**：複雜數學公式可能需要手動修正
4. **Pages 自動化**：需要 Pages.app 且可能彈出視窗

## 問題排除

### OCR 辨識不準確
- 確認原始 Word 文字清晰
- 嘗試調整截圖解析度（程式碼內 dpi 參數）

### Pages 轉換失敗
- 確認 Pages.app 已安裝
- 確認有螢幕存取權限（System Preferences → Security）

---

*小豪編輯部 2026*
